# Additional dependencies for data pipeline
# Install with: pip install -r requirements-pipeline.txt

# Document conversion
pypdf>=4.2.0              # PDF text extraction (already in requirements.txt)
python-frontmatter>=1.1.0  # Markdown front-matter handling (already in requirements.txt)
docx2txt>=0.8              # Word documents
python-pptx>=0.6.21        # PowerPoint presentations
openpyxl>=3.1.2           # Excel files (xlsx)
xlrd>=2.0.1               # Excel files (xls)
pandas>=2.0.0             # Data processing
tabulate>=0.9.0           # Markdown table generation

# Web and markup
html2text>=2020.1.16      # HTML to markdown
beautifulsoup4>=4.12.0    # HTML parsing
lxml>=4.9.0               # XML/HTML parser

# Images (OCR)
Pillow>=10.0.0            # Image processing
pytesseract>=0.3.10       # OCR (requires tesseract-ocr system package)

# E-books
ebooklib>=0.18            # EPUB handling

# Optional: Audio/Video transcription
# whisper>=1.0            # OpenAI Whisper for transcription (large, GPU recommended)
# pydub>=0.25.1           # Audio processing

